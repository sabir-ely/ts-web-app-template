{
  "name": "{{name}}",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "api:db:migrate": "pnpm --filter @{{name}}/api db:migrate",
    "api:db:types": "pnpm --filter @{{name}}/api db:types",
    "api:db:sync": "api:db:migrate && api:db:types"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.30.2"
}
